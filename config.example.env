# NCBI 권장: tool/email 파라미터를 넣어 트래픽 식별
PUBMED_TOOL=pubmed-mcp
PUBMED_EMAIL=jeechoi@kist.re.kr

# 선택: NCBI API key (있으면 rate limit 완화에 도움)
NCBI_API_KEY=

# 선택: 서버 포트
PORT=8000

# 선택: 요청 타임아웃(초)
HTTP_TIMEOUT=20
